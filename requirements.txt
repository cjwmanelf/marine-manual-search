# =====================================================================
# 설치:  pip install -r requirements.txt
#
# 플랫폼별 안내
#  - 🍎 macOS (애플 실리콘): 이 파일 그대로 설치하면 끝.
#       기본 torch가 MPS(GPU)를 자동 지원합니다. 아래 [GPU 전환] 단계는 하지 마세요.
#  - 🪟 Windows / Linux (CPU): 이 파일 그대로 설치하면 끝.
#  - 🎮 Windows / Linux + NVIDIA GPU: 설치 후 torch만 CUDA 빌드로 교체 → 아래 [GPU 전환] 참고.
# =====================================================================

# 필수 (이미지 + 텍스트 검색)
pymupdf
torch                     # 기본은 CPU 빌드. GPU(CUDA)를 쓰려면 아래 [GPU 전환] 참고
transformers
huggingface_hub
gradio
numpy
pillow
doclayout-yolo
rapidocr-onnxruntime      # 스캔본 PDF OCR (한글+영어, 시스템 바이너리 불필요)

# 대안 OCR (선택): rapidocr 대신 쓰려면 아래 주석 해제
#   easyocr                # 한글+영어 (torch 사용)

# [GPU 전환] Windows/Linux + NVIDIA GPU(예: RTX 3060)에서만. (맥은 불필요 — MPS 자동)
#   pip uninstall -y torch
#   pip install torch --index-url https://download.pytorch.org/whl/cu130
#   (Python 3.14 / torch 2.13.0+cu130 지원 확인됨. 드라이버 610.62면 별도 CUDA 툴킷 불필요)
