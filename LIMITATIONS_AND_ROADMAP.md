# 🧭 한계 · 다음 단계 · 데모 배포

> PoC의 한계와 다음 단계를 정리하고, 이미 수행한 확장과 **데모 배포** 방법을 기록.

---

## 1. 현재 PoC의 한계

| 구분 | 한계 |
|---|---|
| 평가 표본 | 검증 질의 15개 · 매뉴얼 1종 → 경향 확인 수준 (통계적 일반화엔 부족) |
| 이미지 정확도 | 도면·표 **이미지 검색 정확도는 정량화 못 함**(정답 라벨링 난이도) — 텍스트만 hit@5 측정 |
| OCR 의존 | 스캔 품질이 나쁘면 글자 인식 오류 → 텍스트 검색 누락 가능 |
| LLM 부재 | **검색까지만** 구현. "문장 답변·요약"은 아직 없음(사람이 결과를 읽어 판단) |
| 청킹 방식 | 글자 수 기준 분할 → 다단(2단)·복잡 레이아웃에선 최적이 아님 |
| 교차언어 점수 | 한글↔영문 코사인 점수대가 낮아(0.5~0.6) 임계값 튜닝이 필요 |

---

## 2. 다음 단계 (로드맵)

- [ ] **로컬 LLM 연결 → RAG**: 검색된 청크를 근거로 **문장 답변 + 출처 페이지** 생성 (완전 로컬 유지)
- [ ] **레이아웃 기반 청킹**: DocLayout-YOLO의 텍스트 블록으로 문단 단위 청킹 (다단·스캔본 정밀도↑)
- [ ] **리랭킹 추가**: 1차 임베딩 검색 후 cross-encoder로 재정렬 → 상위 정확도↑
- [ ] **도면 평가셋 구축**: 도면 질의-정답 라벨을 만들어 **이미지 검색 정확도 정량화**
- [ ] **도메인 파인튜닝**(향후): 선박기기 용어로 임베딩 모델 파인튜닝 → 교차언어 점수 개선
- [ ] **기록(로그) 통합**: 매뉴얼뿐 아니라 과거 정비 기록까지 색인해 통합 검색

---

## 3. 이미 수행한 추가 시도 — 파이프라인 확장 이력

PoC를 한 번에 만든 게 아니라, 한계를 발견할 때마다 **반복적으로 확장**했다.

| 영역 | 확장 이력 |
|---|---|
| 텍스트 검색 | 키워드 매칭 → 의미 임베딩(e5) → **다국어(bge-m3)** (한글 질의 대응) |
| 이미지 색인 | 페이지 전체 → **도면·표 크롭(DocLayout-YOLO)** (정밀도↑) |
| 색인 단위 | 페이지 → **청크 단위** (긴 페이지 누락·희석 해결) |
| 결과 표시 | 짧은 발췌 → **페이지 이미지 + 청크 전문** |
| 세트 관리 | 단일 색인 → **다중 세트 + append/덮어쓰기**(찌꺼기 자동 정리) |
| 실행 환경 | CPU 고정 → **CUDA / Apple MPS / CPU 자동 감지**, OCR 신뢰도 조절 |

> 모델 선정 근거는 [`MODEL_SELECTION.ipynb`](MODEL_SELECTION.ipynb), 개선 효과 수치는 [`EVALUATION.ipynb`](EVALUATION.ipynb) 참고.

---

## 4. 데모 배포

혼자 로컬로 쓰는 것 외에, 다른 사람과 공유하는 방법을 지원한다. (GitHub는 코드 저장소일 뿐, 앱 실행은 아래 방식으로)

### 실행 옵션 (app.py)
| 옵션 | 설명 |
|---|---|
| (없음) | 로컬 전용 `http://127.0.0.1:7860` (기본) |
| `--host 0.0.0.0` | 사내망(LAN) 공유 |
| `--share` | 임시 공개 링크 생성 |
| `--open` | 공유 모드에서도 로컬 브라우저 자동 열기 |
| `--port <번호>` | 포트 변경 (기본 7860) |

Windows는 `run.bat`(로컬) / `run_share.bat`(공유 링크 + 로컬 자동 열기) 더블클릭으로도 실행.

### 4-1. 사내망(LAN) 공유 — 실제 업무용 (권장) ✅ 동작 검증됨
```bash
python app.py --host 0.0.0.0
```
- 같은 네트워크의 다른 PC에서 **`http://<내 PC IP>:7860`** 으로 접속 (IP는 `ipconfig`로 확인).
- **외부 통신 없이 사내망 안에서만** 공유 → 보안 요건 부합. **실제 선박 매뉴얼은 이 방식으로만.**
- 처음 실행 시 Windows 방화벽 팝업 → **"액세스 허용"**(사설 네트워크) 해야 다른 PC에서 접속됨.
- *검증*: LAN IP `192.168.x.x:포트` 접속 HTTP 200 확인.

### 4-2. 임시 공개 링크 — 발표·시연용 ✅ 동작 검증됨
```bash
python app.py --share            # 또는 run_share.bat 더블클릭
```
- 실행 중인 내 PC로 연결되는 **임시 공개 URL(`https://xxxx.gradio.live`, 최대 1주일 best effort)** 생성.
- 별도 서버 없이 즉시 시연 가능. 앱이 켜져 있는 동안만 유효.
- ⚠️ **비밀번호 없음 → 사내 매뉴얼은 올리지 말 것**(링크 아는 누구나 접근). 공개 데모는 샘플/공개 PDF만.
- *검증*: `gradio.live` 링크 생성 + 외부 접속 HTTP 200 확인.

### 4-3. 상시 공개 데모 — Hugging Face Spaces (추후, 디벨롭 후 시도)
- `app.py` + `requirements.txt` 만 올리면 Gradio Space로 **상시 실행** 가능.
- 절차(예정): HF 계정 → **New Space → SDK: Gradio** → 저장소 파일 업로드(또는 GitHub 연동) → 자동 빌드.
- **기능 고도화(RAG 등) 디벨롭 이후에 시도할 예정.** (무료 CPU 티어는 bge-m3 ~2GB로 느릴 수 있어 샘플 PDF 전용 운영 필요)

### 보안 원칙
- **실제 선박 매뉴얼(사내 자료)은 공개 배포 금지.**
- 공개 데모 = 샘플 PDF, **실제 업무 = 사내 자체 호스팅(4-1)** 로 분리한다.

---

## 5. 요약

- **한계**를 6가지로 명시하고, **다음 단계 6가지**(핵심: 로컬 LLM 기반 RAG)를 로드맵으로 정리했다.
- 개발 과정에서 **파이프라인을 6개 영역에서 반복 확장**했다(단순 1회 구현이 아님).
- **데모 배포**를 실제 구현·검증했다: `--host 0.0.0.0`(사내망, HTTP 200) · `--share`(공개 링크, HTTP 200).
- **상시 공개(HF Spaces)는 기능 디벨롭 후 시도** 예정으로 남겨 두었다.
